apiVersion: v1
kind: Service
metadata:
  name: {{ include "wellq.fullname" . }}-web
  labels:
    {{- include "wellq.labels" . | nindent 4 }}
    component: web
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "wellq.selectorLabels" . | nindent 4 }}
    component: web







